services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENV=local
      - LOG_LEVEL=INFO
      - LOG_FORMAT=console
    restart: unless-stopped
